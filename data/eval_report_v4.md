# RAGAS Evaluation Report

> Generated by `scripts/report.py` from `eval_results.json`.

## Aggregate Scores

| Metric | Baseline | Agent | Delta |
|--------|----------|-------|-------|
| `faithfulness` | 1.0000 | 0.8752 | ▼ 0.1248 |
| `answer_relevancy` | 0.6962 | 0.7378 | ▲ 0.0416 |
| `context_precision` | 0.7500 | 0.7275 | ▼ 0.0225 |
| `context_recall` | 0.5667 | 0.7333 | ▲ 0.1667 |

## Per-Question Breakdown

### Q1: Im trying to deploy a hosted agent in italy north and it uses file search. I heard cold starts can be bad so how do I turn those off? Also is italy north even allowed for this?

| Metric | Baseline | Agent |
|--------|----------|-------|
| `faithfulness` | 1.0000 | 1.0000 |
| `answer_relevancy` | 0.7817 | 0.8694 |
| `context_precision` | 0.0000 | 0.0000 |
| `context_recall` | 0.0000 | 0.0000 |

**Baseline answer:**
> Italy North is a supported region for Hosted Agents [1].

Regarding your other questions:

*   I could not find relevant documentation in the knowledge base regarding how to turn off cold starts.
*   I could not find relevant documentation in the knowledge base regarding file search functionality.

**Agent answer:**
> Based on the documentation provided:

*   **Region Availability:** Yes, Italy North is listed as a supported region for Hosted Agents [1].
*   **Cold Starts:** I could not find relevant documentation in the knowledge base regarding how to prevent or turn off cold starts for a hosted agent.

---
### Q2: my agent could connect to azure storage fine while I was developing but as soon as I published it Im getting unauthorized errors everywhere. tool connection audience is definitely https://storage.azure.com so what gives?? how do i fix

| Metric | Baseline | Agent |
|--------|----------|-------|
| `faithfulness` | 1.0000 | 0.9091 |
| `answer_relevancy` | 0.5848 | 0.5696 |
| `context_precision` | 1.0000 | 0.8042 |
| `context_recall` | 0.3333 | 0.6667 |

**Baseline answer:**
> The unauthorized errors you are encountering after publishing your agent are likely related to the change in the agent's identity and required permissions for accessing downstream resources like Azure Storage [1], [3].

Here are the steps and concepts to address the issue:

1.  **Check the Agent Identity:** When an agent is published, it uses a distinct identity, which is different from the shared project identity used during development [4]. You must ensure that permissions are granted to this new, published agent identity [4].
2.  **Reassign RBAC Roles:** If tool calls fail after publishing, the likely cause is that the Agent Application identity does not have the same access to the downstream resources as the project identity did [1], [3]. You must reassign the required Role-Based Access Control (RBAC) roles to the published agent identity for any Azure resources it needs to access, such as Azure Storage [1], [3].
3.  **Authentication Method:** Remember that invoking Agent Applications requires using Microsoft Entra ID (Azure RBAC) for authorization, as API key authentication is not supported [5].
4.  **Token Issues:** If the error is specifically a `401 Unauthorized` when invoking the endpoint, the cause may be that the access token is missing, expired, or for the wrong resource. In this case, you should reauthenticate and request a token for `https://ai.azure.com` [1], [3].

**Agent answer:**
> The issue likely stems from the change in the agent's identity after publishing, which requires explicit role reassignment for downstream resources like Azure Storage [1], [3].

To resolve the unauthorized errors, consider the following steps:

1.  **Reassign RBAC Roles to the Published Identity:**
    *   When an agent is published, the Agent Application identity is distinct from the project identity [1], [4].
    *   You must reassign the required Role-Based Access Control (RBAC) roles to the **published agent identity** for the downstream Azure resources it needs to access (e.g., Azure Storage) [1], [3].
    *   Ensure the agent identity has the necessary RBAC role on the target resource [4].

2.  **Check Authentication/Token:**
    *   If the error is specifically `401 Unauthorized` when invoking the endpoint, the access token may be missing, expired, or for the wrong resource [1], [3]. In this case, you should reauthenticate and request a token for `https://ai.azure.com` [1], [3].

**Summary of Identity Best Practices:**
*   Confirm that the permissions are granted to the current identity used by the agent (the distinct identity for published agents) [4].
*   The published agent identity must have the required RBAC roles on the target resource [1], [3].

---
### Q3: migrating python assistant from classic foundry... old way used threads, runs, and the project client. whats the new way to handle conversations and run execution?

| Metric | Baseline | Agent |
|--------|----------|-------|
| `faithfulness` | 1.0000 | 1.0000 |
| `answer_relevancy` | 0.5934 | 0.6520 |
| `context_precision` | 0.8333 | 0.8333 |
| `context_recall` | 1.0000 | 1.0000 |

**Baseline answer:**
> *   **Handling Runs vs. Responses:**
    *   Previously, runs were asynchronous processes that executed against threads [1].
    *   The new approach uses responses, which are simpler: you provide a set of input items to execute and receive a list of output items back [1].
    *   Responses can be used independently or with conversation objects to store context [1].
    *   The responses API utilizes the OpenAI client [1].

*   **Handling Threads vs. Conversations:**
    *   Previously, threads stored messages on the server side [4].
    *   A conversation can store various items, including messages, tool calls, tool outputs, and other data [4].
    *   The current method for creating conversations uses the OpenAI client obtained from `project.get_openai_client()` [3].
    *   Using a conversation is more flexible than the older thread-based pattern, where state was tightly coupled to thread objects [2].

*   **Migration Process:**
    *   A migration tool is available on GitHub to help automate the migration of your agents and assistants [2].
    *   This tool can migrate code constructs such as agent definitions, thread creation, message creation, and run creation [2].
    *   However, the tool does not migrate state data, such as past runs, threads, or messages [2].

**Agent answer:**
> The migration involves moving from the older thread-based pattern and asynchronous runs to using responses and conversations [1], [4], [5].

**Handling Conversations (State):**
*   **Old Way:** State was managed using threads, which stored messages on the server side [4], [3].
*   **New Way:** Conversations can store various items, including messages, tool calls, tool outputs, and other data [4]. The current approach for conversation creation uses the OpenAI client obtained from `project.get_openai_client()` [4].
*   **Flexibility:** For multi-turn flows, you can build the flow by using the output from a previous response as the starting point for the next request, which provides more flexibility than the older thread-based pattern [5].

**Handling Execution (Runs):**
*   **Old Way:** The previous approach used runs, which were asynchronous processes that executed against threads [1], [3].
*   **New Way:** The responses API is used instead, which is simpler; you provide a set of input items to execute and receive a list of output items back [1]. This new approach calls `responses.create()` on the OpenAI client [1].

---
### Q4: Im dumping a 2,000,000 character string into a single message to my agent and it crashes. what limit did I hit? how can i bypass this

| Metric | Baseline | Agent |
|--------|----------|-------|
| `faithfulness` | 1.0000 | 0.6667 |
| `answer_relevancy` | 0.6090 | 0.6638 |
| `context_precision` | 0.9167 | 1.0000 |
| `context_recall` | 0.5000 | 1.0000 |

**Baseline answer:**
> The limit you encountered is related to the message content size, which is identified as `content_size_exceeded` [2].

To address this, the documentation recommends the following actions:

*   **Send smaller messages** [2], [4].
*   **Move the large content into uploaded files and use file search** [2], [4].

For example, when creating a message, if the `text` content is too large, you can send smaller messages or move the large content into files and utilize file search [4].

**Agent answer:**
> You hit the "Message content size" limit [1], [2].

According to the default limits, the maximum size for `text` content per message is 1,500,000 characters [2]. When this limit is exceeded, the error code returned is `content_size_exceeded` [2].

To bypass this limit, you can take the following actions:

*   Send smaller messages [1].
*   Move the large content into uploaded files and use file search [1], [2].

---
### Q5: setting up mcp server and an a2a endpoint connection. need to use agent identity for auth. what are the exact auth type values for each?

| Metric | Baseline | Agent |
|--------|----------|-------|
| `faithfulness` | 1.0000 | 0.8000 |
| `answer_relevancy` | 0.9121 | 0.9345 |
| `context_precision` | 1.0000 | 1.0000 |
| `context_recall` | 1.0000 | 1.0000 |

**Baseline answer:**
> To connect an MCP server or an A2A endpoint using agent identity authentication, the required authentication type values are:

*   **MCP server:** `AgenticIdentityToken` [1]
*   **A2A endpoint:** `AgenticIdentity` [1]

**Agent answer:**
> To connect an MCP server or an A2A endpoint using agent identity authentication, you must create a project connection that specifies the following authentication type values:

*   **MCP server:** `AgenticIdentityToken` [1], [2]
*   **A2A endpoint:** `AgenticIdentity` [1], [2]

When configuring the connection, the authentication type depends on the tool type [1], [2].

---