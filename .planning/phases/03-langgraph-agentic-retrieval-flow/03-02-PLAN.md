---
wave: 2
depends_on: ["03-01-PLAN.md"]
files_modified:
  - "src/agent.py"
autonomous: true
---

# Plan 2: Grade, Generate, and Hallucination Nodes

## Goal
Implement structured LLM relevance grading, generation with citations, and hallucination checking.

## Tasks

<task>
<read_first>
- src/agent.py
- .planning/phases/03-langgraph-agentic-retrieval-flow/03-AI-SPEC.md
</read_first>
<action>
In `src/agent.py`, add `grade_node(state: AgentState)`:
1. Define Pydantic model `GradeResult(BaseModel)` with `is_relevant: bool`.
2. Iterate through `state["documents"]`.
3. For each document, prompt `gemma4:e4b` via structured output (temp=0.0): "Assess if this document contains keywords or semantic meaning relevant to the question. Return True or False."
4. Filter out False documents.
5. Return `{"documents": filtered_documents}`.
</action>
<acceptance_criteria>
- `src/agent.py` contains `def grade_node(state: AgentState):`
- Code uses a boolean / Pydantic model `GradeResult` to determine relevance as explicitly required.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/agent.py
- .planning/phases/03-langgraph-agentic-retrieval-flow/03-CONTEXT.md
</read_first>
<action>
In `src/agent.py`, add `generate_node(state: AgentState)`:
1. Combine `state["documents"]` into a single context string. Include metadata paths in the text dynamically.
2. Prompt `gemma4:e4b` to answer `state["question"]` using ONLY the provided context.
3. System prompt MUST enforce citation style explicitly as per context: "Use inline citations like [1] and append a numbered footnote map at the bottom of the response tying into the source paths."
4. If documents list is empty (can happen if retries were hit but grading failed), generate a graceful "I could not find relevant documentation in the knowledge base." message.
5. Return `{"generation": generated_text}`.
</action>
<acceptance_criteria>
- `src/agent.py` contains `def generate_node(state: AgentState):`
- System prompt contains the exact citation instructions "inline citations... numbered footnote map at the bottom".
</acceptance_criteria>
</task>

<task>
<read_first>
- src/agent.py
</read_first>
<action>
In `src/agent.py`, add `check_hallucination(state: AgentState) -> str`:
1. Define Pydantic model `HallucinationCheck(BaseModel)` with `is_grounded: bool`.
2. Prompt `gemma4:e4b` to determine if `state["generation"]` is fully grounded by `state["documents"]`.
3. If grounded and addresses the question, return `"useful"`.
4. If hallucinated or unuseful, return `"not_supported"`.
</action>
<acceptance_criteria>
- `src/agent.py` contains `def check_hallucination`
- Function returns string routes `"useful"` or `"not_supported"`.
</acceptance_criteria>
</task>

## Verification
- Requirements satisfied: RAG-04, RAG-05, RAG-06, RAG-08.
- must_haves: Grading filters low-relevance chunks and generation is equipped with proper citations.
