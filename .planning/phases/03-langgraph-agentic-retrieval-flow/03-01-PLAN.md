---
wave: 1
depends_on: []
files_modified:
  - "src/config.py"
  - "src/agent.py"
autonomous: true
---

# Plan 1: State Schema, Retrieve & Router Nodes

## Goal
Establish LangGraph `AgentState`, integrate ChromaDB retrieval, and build the out-of-scope router node.

## Tasks

<task>
<read_first>
- src/config.py
- .planning/phases/03-langgraph-agentic-retrieval-flow/03-CONTEXT.md
- .planning/phases/03-langgraph-agentic-retrieval-flow/03-AI-SPEC.md
</read_first>
<action>
Modify `src/config.py` to ensure `LLM_MODEL` is set to `"gemma4:e4b"` overriding any previous settings.
Create `src/agent.py`. Add the following imports:
```python
from typing import TypedDict, Annotated, Sequence
import operator
from langgraph.graph import StateGraph, END
from pydantic import BaseModel, Field
```
Define the `AgentState` TypedDict containing:
- `question: str`
- `generation: str`
- `documents: list[str]`
- `retries: int` # Tracks re-retrieval loops
</action>
<acceptance_criteria>
- `src/config.py` contains `LLM_MODEL = "gemma4:e4b"`
- `src/agent.py` contains `class AgentState(TypedDict):` with exactly formatting specified.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/agent.py
- src/vectorstore.py
</read_first>
<action>
In `src/agent.py`, import `ChromaWrapper` from `src.vectorstore` and `config`.
Create the `retrieve_node(state: AgentState)` function:
1. Read `question` from state.
2. Initialize `ChromaWrapper`.
3. Call `similarity_search(query=question, k=4)` to retrieve chunks.
4. Extract text formatting (including chunk metadata as text to be passed to LLM) from the `Document` objects and return them as `{"documents": [docs]}`.
</action>
<acceptance_criteria>
- `src/agent.py` contains `def retrieve_node(state: AgentState):`
- `retrieve_node` returns `{"documents": ...}` containing retrieved context.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/agent.py
- .planning/phases/03-langgraph-agentic-retrieval-flow/03-CONTEXT.md
</read_first>
<action>
In `src/agent.py`, create the routing logic. 
Define a Pydantic model `RouteQuery(BaseModel)` with `is_relevant: bool = Field(...)`.
Create `route_query(state: AgentState)` function.
Use LangChain's Ollama integration with `gemma4:e4b` (temperature=0.0).
System prompt MUST enforce strictly out-of-scope routing:
"You are an expert router. Route the user question to either 'Azure AI Foundry Agents' (True) or 'Out of Scope' (False). Ignore general Azure or general IT questions."
Output formatting should use Pydantic structure to get the boolean safely.
Return `"retrieve"` if True, `"out_of_scope"` if False.
</action>
<acceptance_criteria>
- `src/agent.py` contains `def route_query`
- Prompt contains instructions clamping explicitly to Azure AI Foundry Agents as described.
</acceptance_criteria>
</task>

## Verification
- Run `python -c "import src.agent; print(dir(src.agent))"` ensuring no syntax errors.
- Requirements satisfied: RAG-01 (partial), RAG-02, RAG-03.
- must_haves: LangGraph components imported and State Schema strictly typed.
